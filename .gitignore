# ┌──────────────────────────────────────────────────────────────┐
# │                     Common ignores                           │
# └──────────────────────────────────────────────────────────────┘

# Dependencies
node_modules/
.pnpm-store/
bun.lockb
yarn.lock          # ← keep only if you don't use yarn
package-lock.json  # ← keep only if you don't use npm

# Build output & dist folders
dist/
build/
out/
public/build/
www/
*.bundle.*
*.min.js
*.min.css

# Development & editor files
.vscode/
.idea/
*.sublime-*
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.local
.env.*.local
.env.development
.env.production
.env.development.local
.env.test.local
.env.production.local

# Testing & coverage
coverage/
.nyc_output/
cypress/videos/
cypress/screenshots/

# Logs & temporary files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lighthouse-report-*.html
*.log

# PWA / Workbox / Service Worker related generated files
workbox-*.js
sw.js
workbox-*.js.map
service-worker.js   # ← only if you generate it automatically
precache-manifest.*.js
manifest.*.json     # ← if you generate manifest dynamically

# Local development certificates (mkcert, etc)
localhost.pem
localhost-key.pem

# MacOS / Windows junk
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
ehthumbs.db
[Tt]humbs.db

# Optional: keep these if you don't want to commit icons you generate
/icons/*.png
/icons/*.ico
/icons/*.svg    # ← only if you generate them automatically

# Optional: if using Vite / Parcel / esbuild / Rollup etc
.cache/
.parcel-cache/
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rspack-cache/
.temp/
tsconfig.tsbuildinfo